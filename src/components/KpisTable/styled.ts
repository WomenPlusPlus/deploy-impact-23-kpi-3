import styled from 'styled-components';
import {Card, Space} from 'antd';

export const SKpiTableCard = styled(Card)`
  min-width: 100%;
  max-width: 100%;
  padding: 3rem;
`;
export const SSpace = styled(Space)`
	display: flex;
	justify-content: center;
`;